import SectionTitle from "../../Shared/SectionTitle/SectionTitle";

const Payment = () => {
    return (
        <div className="pt-10">
            <SectionTitle title="payment"  subtitle="confirm your order" />
            <h2>Payment</h2>
        </div>
    );
};

export default Payment;